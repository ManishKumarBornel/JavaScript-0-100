/* function is a defined set of statements that performs a task or calculates a value

function functionname(declaration)
{
    function to be performed;
    return statement;
}
    console.log(funcitonname(declaration))  //calling function

*/

function hey(name)
{
    return "HEY! " + name + " How are you?";
}
console.log(hey("Manish"));
