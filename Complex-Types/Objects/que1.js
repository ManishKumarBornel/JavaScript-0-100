/*
    Write a function that takes a 
    user as an input and greets them with their name and age
*/

function greet(name)
{
    console.log("hi " + user.name + " your age is "+ user.age);
}
let user = {
    name : "Manish",
    age : 18
}   

greet(user);